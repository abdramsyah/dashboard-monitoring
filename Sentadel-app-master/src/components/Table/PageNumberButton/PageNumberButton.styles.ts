import Colors from '@sentadell-src/config/Colors';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    width: 36,
    height: 36,
    borderWidth: 1,
    borderColor: Colors.base.oxfordBlue,
    borderRadius: 36 / 2,
    alignItems: 'center',
    justifyContent: 'center'
  }
});
