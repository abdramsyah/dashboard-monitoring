import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  groupContainer: { padding: 8 },
  groupTitle: { fontWeight: '700', marginBottom: 10 },
  wrapContainer: {
    flexDirection: 'row',
    gap: 16
  },
  baseContentContainerStyle: {
    flexGrow: 1,
    paddingBottom: 60,
    gap: 10
  }
});
